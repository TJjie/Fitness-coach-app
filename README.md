# Incoming project

**CoachOS** lives in [`coach-app/`](./coach-app/). See [`coach-app/README.md`](./coach-app/README.md) for local run and Vercel deploy steps.

When connecting this repo to Vercel, set the project **Root Directory** to **`coach-app`**.
