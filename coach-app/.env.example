# Optional: light access phrase for the whole app (same for every visitor).
# Set in Vercel → Project → Settings → Environment Variables for Production.
# Leave unset for no gate. Not suitable for real secrets — it ships in the JS bundle.
# VITE_ACCESS_PASSWORD=
