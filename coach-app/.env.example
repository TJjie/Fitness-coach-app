# Supabase (required for coach sign-in and cloud data)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=

# Optional: override clients table PK column name if not `id`
# VITE_SUPABASE_CLIENTS_ID_COLUMN=

# Optional: PK column names if not `id`
# VITE_SUPABASE_AVAILABILITY_SLOTS_ID_COLUMN=
# VITE_SUPABASE_BOOKINGS_ID_COLUMN=
